import {
	StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
	container: {
		width: '100%',
		marginTop: 40,
		height: 30,
		backgroundColor: '#F44444',
		paddingTop: 4,
	},
});

export default styles;
